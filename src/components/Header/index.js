import React from 'react';

import { Wrapper, Container, Logo } from './styles';

const Header = () => {
  return (
    <Wrapper>
      <Container>
        <Logo />
      </Container>
    </Wrapper>
  );
};

export default Header;
